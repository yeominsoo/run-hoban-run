# 무한 러너 구현 노트 (2026-07-14)

## 원본 스펙 (v1)

- 목표: 자동으로 달리는 캐릭터가 장애물을 피하며 최대한 오래 달리기
- 입력: 탭/클릭 = 점프, 스와이프 다운 / 빠른 탭 2회 = 슬라이드
- 규칙: 속도 지속 상승, 코인 수집 시 +10점
- 장애물: 낮은 장애물(점프), 높은 장애물(슬라이드), 구덩이(타이밍 점프)
- 점수: 달린 거리(1m = 1점) + 코인
- Canvas 사용, 모바일 주의사항 없음

## 결정과 이유

1. **목숨 수: 1개(단일 충돌 즉시 종료)**. 스펙에 HP/목숨 언급이 없다. 무한 러너 장르
   표준(Chrome Dino 등)이 "한 번 부딪히면 끝"이라 별도 HP 시스템을 새로 만들지 않았다 —
   다른 게임들(HP 3)과 다른 이유가 명확할 때만 다르게 가는 편이 낫다고 판단했다.
2. **"속도 지속 상승"을 다른 게임과 다르게 연속 함수로 구현**: 볼 피하기·타이핑 생존 등은
   "N초마다 계단식으로 배율 증가"였는데, 이 게임은 스펙이 명시적으로 "지속 상승"이라고
   표현했다 — 다른 게임의 계단식 스펙과 문구가 다르므로 의도적으로 매 프레임
   `speed += ACCEL * dt`로 부드럽게 증가시켰다. 이 저장소에서 처음 나온 연속 가속 패턴.
3. **점프는 물리 기반(중력+초기속도), 슬라이드는 판정 박스 축소+타이머**: 점프 궤적은
   `y' = v, v' = -GRAVITY`로 시뮬레이션해 자연스러운 포물선을 만들고, 슬라이드는 별도
   물리 없이 "일정 시간 동안 캐릭터 높이만 낮아짐" 상태로 단순화했다 — 슬라이드까지
   물리로 구현하면 복잡도만 늘고 체감 차이가 없다.
4. **빠른 탭 2회 판정**: 마지막 탭 시각과 현재 탭 시각의 차이가 300ms 이내면 "빠른 탭 2회"로
   보고 슬라이드를 발동한다. 300ms는 일반적인 더블클릭/더블탭 인식 기준값(OS 기본값들이
   대체로 300~500ms 범위)에서 반응성을 우선해 짧은 쪽을 택했다.
5. **장애물 3종의 구체적 모양**:
   - 낮은 장애물: 바닥에 붙은 블록 → 점프해야 통과
   - 높은 장애물: 머리 높이에 떠 있는 막대 → 슬라이드해야 통과(서 있으면 부딪힘)
   - 구덩이: 바닥 자체가 끊긴 구간 → 점프 중(공중)이어야 안전, 착지 지점이 구덩이 안이면
     추락 판정
6. **구덩이 폭을 시간에 따라 넓히지 않음**: 점프의 체공 시간은 고정(물리 상수)이지만 이동
   속도는 계속 오르므로, "속도×체공시간"으로 계산되는 실제 점프 사거리는 시간이 지날수록
   저절로 늘어난다. 따라서 구덩이 폭을 고정해두면 난이도가 저절로 유지되고, 오히려 속도
   자체가 반응 시간을 줄이는 쪽으로 난이도를 올린다 — 별도로 구덩이를 더 넓힐 필요가 없다고
   판단했다.
7. **점수 환산**: "1m = 1점"을 위해 화면 픽셀과 "미터"를 잇는 임의 환산값이 필요했다 —
   `50px = 1m`로 정하고 `달린 거리 점수 = floor(누적 스크롤 거리 / 50)`으로 계산한다.
   코인은 스펙대로 개당 +10점을 별도로 더한다(거리 점수와 코인 점수를 합산해 최종 점수).
8. **장애물 스폰 간격**: 시간이 지날수록(=속도가 오를수록) 장애물 사이 최소 간격을 서서히
   줄여 밀도를 높인다 — 속도 상승만으로는 "장애물이 더 빨리 스쳐 지나갈 뿐" 밀도 자체는
   그대로라 나중에도 헐거운 구간이 계속 나올 수 있어, 밀도 증가를 별도 축으로 추가했다.
9. **랭킹 UI**: 완료된 7개 게임과 동일한 공용 패턴(`shared/leaderboard.ts`,
   `shared/game-shell.css`)을 처음부터 포함해서 만든다.

## 자동 플레이 테스트로 발견한 버그: 점프로 높은 장애물을 넘어갈 수 있었음

"낮은 장애물은 점프, 높은 장애물은 슬라이드"라는 스펙을 구현하면서, 높은 장애물을 단순히
"바닥에서 좀 띄운 30px짜리 블록"으로만 만들었다. 그런데 점프 최고 높이가 약 131px로 이
블록(바닥에서 30~60px 높이)보다 훨씬 높아서, 타이밍만 맞으면 슬라이드 없이 그냥 점프로도
넘어가버렸다 — "장애물마다 정해진 유일한 통과 방법이 있다"는 스펙의 의도가 깨지는 버그였다.
직접 여러 장애물 유형을 골고루 만나며 15초간 살아남는 자동 플레이 Playwright 테스트를 짜서
검증하는 과정에서 발견했다(수동으로 몇 번 눌러보는 것만으로는 우연히 안 걸렸을 수도 있다).

**수정**: 높은 장애물의 충돌 판정 사각형을 "슬라이드 통로(clearance)만 남기고 화면 맨 위까지
뻗은 기둥"으로 바꿨다(`top: 0, height: groundY - clearance`). 이러면 점프의 최고 높이가
아무리 높아도 항상 기둥과 겹치기 때문에 슬라이드만이 유일한 통과 방법이 된다. 시각적으로는
위쪽을 그라디언트로 흐리게 처리해 "천장 너머로 이어지는 기둥"처럼 보이게 했다.
**교훈**: 이런 종류의 "장애물 A는 반드시 액션 X로만 피해야 한다"는 규칙은, 회피 액션이
만들 수 있는 모든 상태(이 경우 점프의 전체 높이 범위)를 실제로 계산해서 장애물의 판정
범위가 그 전체 범위를 덮는지 확인해야 한다 — 대충 "장애물을 좀 낮게 두면 되겠지"로는
부족했다.

## 실사용자 버그 리포트로 발견한 버그: 더블탭 슬라이드가 사실상 항상 실패함 (2026-07-14)

배포 후 사용자가 "코인 먹으려다가 낭떠러지로 떨어짐, 아래로 통과해야 하는(슬라이드로
넘기는) 장애물인데 탈락 처리됨"이라고 제보했다. 원인을 추적해보니 "빠른 탭 2회 = 슬라이드"
자체가 물리적으로 성립할 수 없는 조건이었다:

- 점프의 체공 시간(`2 * |JUMP_VELOCITY| / GRAVITY` ≈ 690ms)이 더블탭 판정 창
  (`DOUBLE_TAP_WINDOW_MS` = 300ms)보다 길다.
- 기존 `triggerSlide()`는 `playerState !== 'running'`이면 그냥 무시했다. 그런데
  더블탭의 첫 번째 탭은 항상 즉시 `triggerJump()`를 발동시켜 플레이어를 공중에 띄운다
  (탭 시점엔 "이게 더블탭의 시작인지" 알 방법이 없으므로 당연히 첫 탭은 무조건 점프로
  처리된다). 그 직후(300ms 이내) 두 번째 탭이 도착해도, 점프가 끝나 착지하기까지는
  최소 690ms가 걸리므로 그 시점엔 항상 `playerState==='jumping'`이라 슬라이드 호출이
  가드에 막혀 아무 일도 안 일어났다 — 즉 더블탭 슬라이드는 실전에서 **한 번도 성공할
  수 없는 죽은 기능**이었다. 사용자는 높은 장애물 앞에서 슬라이드하려고 빠르게 두 번
  탭했지만 실제로는 (무시된 두 번째 탭 대신) 첫 탭의 점프만 발동해 공중에 뜬 채 높은
  장애물의 전체 높이 판정 기둥에 그대로 부딪힌 것이다.

**수정**: `triggerSlide()`가 `playerState==='jumping'`일 때도 동작하도록 바꿨다 — 이
경우 점프를 취소하고 즉시 바닥으로 착지시킨 뒤 슬라이드를 시작한다("다이빙" 느낌). 이러면
더블탭의 두 번째 탭이 실제로 의미 있는 동작(점프 취소 + 슬라이드 전환)을 만들어낸다.

**교훈**: "두 입력이 300ms 이내면 조합 동작으로 인식한다" 같은 더블탭 규칙을 만들 때는,
첫 입력이 만드는 상태 전이가 그 판정 창보다 오래 지속되지 않는지 반드시 확인해야 한다 —
여기서는 "첫 탭이 만드는 점프 상태(690ms)"가 "판정 창(300ms)"보다 길어서, 판정 창이
사실상 죽어있는 코드였는데도 타입체크·빌드·기존 자동 플레이 테스트 전부 통과했다(자동
플레이 테스트는 스와이프 다운으로만 슬라이드를 검증했지 더블탭 경로는 애초에 테스트하지
않았다). 사용자 실사용 리포트가 아니었다면 계속 몰랐을 것 — 입력 조합 로직은 상태 전이
지속 시간까지 같이 명시적으로 테스트해야 한다는 교훈을 얻었다.
