APIKEY=replace-with-openai-api-key
OPENAPI=replace-with-openai-api-key
OPENAI_API=replace-with-openai-api-key
OPENAI_API_KEY=replace-with-openai-api-key

# 가위바위보(/rps) 실시간 매칭 서버 주소. 미설정 시 개발 중에는
# ws://<현재 호스트>:8787/rps 로 자동 접속을 시도한다.
# WAS 배포 후에는 wss://<확정 도메인>/rps 를 지정하고 빌드해야 한다.
# 자세한 내용은 ws-server/README.md 참고.
VITE_RPS_WS_URL=

# 싱글게임 전체 점수 랭킹 HTTP API. 비워두면 VITE_RPS_WS_URL의 호스트에서
# /ranking/score 경로를 자동으로 사용한다.
VITE_SCORE_RANKING_URL=

# 라이어게임(/liar) 실시간 매칭 서버 주소. 미설정 시 개발 중에는
# ws://<현재 호스트>:8787/liar 로 자동 접속을 시도한다.
# rps와 같은 서버 프로세스가 서비스하므로 WAS 배포 후에는 같은 도메인의 /liar 경로를 지정한다.
# 자세한 내용은 ws-server/README.md 참고.
VITE_LIAR_WS_URL=

# 마피아게임(/mafia) 실시간 매칭 서버 주소. 미설정 시 개발 중에는
# ws://<현재 호스트>:8787/mafia 로 자동 접속을 시도한다.
# rps/liar와 같은 서버 프로세스가 서비스하므로 WAS 배포 후에는 같은 도메인의 /mafia 경로를 지정한다.
# 자세한 내용은 ws-server/README.md 참고.
VITE_MAFIA_WS_URL=

# 할리갈리(/halligalli) 실시간 매칭 서버 주소. 미설정 시 개발 중에는
# ws://<현재 호스트>:8787/halligalli 로 자동 접속을 시도한다.
# rps/liar/mafia와 같은 서버 프로세스가 서비스하므로 WAS 배포 후에는 같은 도메인의 /halligalli 경로를 지정한다.
# 자세한 내용은 ws-server/README.md 참고.
VITE_HALLIGALLI_WS_URL=

# 윷놀이(/yutnori) 실시간 매칭 서버 주소. 미설정 시 개발 중에는
# ws://<현재 호스트>:8787/yutnori 로 자동 접속을 시도한다.
# rps/liar/mafia/halligalli와 같은 서버 프로세스가 서비스하므로 WAS 배포 후에는 같은 도메인의 /yutnori 경로를 지정한다.
# 자세한 내용은 ws-server/README.md 참고.
VITE_YUTNORI_WS_URL=

# 전략윷놀이(/strategy-yutnori) 실시간 매칭 서버 주소. 미설정 시 개발 중에는
# ws://<현재 호스트>:8787/strategy-yutnori 로 자동 접속을 시도한다.
# rps/liar/mafia/halligalli/yutnori와 같은 서버 프로세스가 서비스하므로 WAS 배포 후에는 같은 도메인의 /strategy-yutnori 경로를 지정한다.
# 자세한 내용은 ws-server/README.md 참고.
VITE_STRATEGY_YUTNORI_WS_URL=

# 두더지 사냥(/mole-hunt) 실시간 매칭 서버 주소. 미설정 시 개발 중에는
# ws://<현재 호스트>:8787/mole-hunt 로 자동 접속을 시도한다.
# rps/liar/mafia/halligalli/yutnori/strategy-yutnori와 같은 서버 프로세스가 서비스하므로 WAS 배포 후에는 같은 도메인의 /mole-hunt 경로를 지정한다.
# 자세한 내용은 ws-server/README.md 참고.
VITE_MOLE_HUNT_WS_URL=

# 순서 기억 챌린지(/memory-sequence) 실시간 매칭 서버 주소. 미설정 시 개발 중에는
# ws://<현재 호스트>:8787/memory-sequence 로 자동 접속을 시도한다.
# rps/liar/mafia/halligalli/yutnori/strategy-yutnori/mole-hunt와 같은 서버 프로세스가 서비스하므로 WAS 배포 후에는 같은 도메인의 /memory-sequence 경로를 지정한다.
# 자세한 내용은 ws-server/README.md 참고.
VITE_MEMORY_SEQUENCE_WS_URL=

# 업다운 넘버(/updown-number) 실시간 매칭 서버 주소. 미설정 시 개발 중에는
# ws://<현재 호스트>:8787/updown-number 로 자동 접속을 시도한다.
# rps/liar/mafia/halligalli/yutnori/strategy-yutnori/mole-hunt/memory-sequence와 같은 서버 프로세스가 서비스하므로 WAS 배포 후에는 같은 도메인의 /updown-number 경로를 지정한다.
# 자세한 내용은 ws-server/README.md 참고.
VITE_UPDOWN_NUMBER_WS_URL=

# 구구단 스퍼트(/multiplication-sprint) 실시간 매칭 서버 주소. 미설정 시 개발 중에는
# ws://<현재 호스트>:8787/multiplication-sprint 로 자동 접속을 시도한다.
# rps/liar/mafia/halligalli/yutnori/strategy-yutnori/mole-hunt/memory-sequence/updown-number와 같은 서버 프로세스가 서비스하므로 WAS 배포 후에는 같은 도메인의 /multiplication-sprint 경로를 지정한다.
# 자세한 내용은 ws-server/README.md 참고.
VITE_MULTIPLICATION_SPRINT_WS_URL=

# 홀짝 암산(/odd-even-math) 실시간 매칭 서버 주소. 미설정 시 개발 중에는
# ws://<현재 호스트>:8787/odd-even-math 로 자동 접속을 시도한다.
# rps/liar/mafia/halligalli/yutnori/strategy-yutnori/mole-hunt/memory-sequence/updown-number/multiplication-sprint와 같은 서버 프로세스가 서비스하므로 WAS 배포 후에는 같은 도메인의 /odd-even-math 경로를 지정한다.
# 자세한 내용은 ws-server/README.md 참고.
VITE_ODD_EVEN_MATH_WS_URL=

# 컬러 지시 게임(/color-instruction) 실시간 매칭 서버 주소. 미설정 시 개발 중에는
# ws://<현재 호스트>:8787/color-instruction 로 자동 접속을 시도한다.
# rps/liar/mafia/halligalli/yutnori/strategy-yutnori/mole-hunt/memory-sequence/updown-number/multiplication-sprint/odd-even-math와 같은 서버 프로세스가 서비스하므로 WAS 배포 후에는 같은 도메인의 /color-instruction 경로를 지정한다.
# 자세한 내용은 ws-server/README.md 참고.
VITE_COLOR_INSTRUCTION_WS_URL=

# 합이 10 퍼즐(/sum-ten-puzzle) 방 모드 실시간 매칭 서버 주소(로컬 연습 모드는 서버 불필요).
# 미설정 시 개발 중에는 ws://<현재 호스트>:8787/sum-ten-puzzle 로 자동 접속을 시도한다.
# rps/liar/mafia/halligalli/yutnori/strategy-yutnori/mole-hunt/memory-sequence/updown-number/multiplication-sprint/odd-even-math/color-instruction와 같은 서버 프로세스가 서비스하므로 WAS 배포 후에는 같은 도메인의 /sum-ten-puzzle 경로를 지정한다.
# 자세한 내용은 ws-server/README.md 참고.
VITE_SUM_TEN_PUZZLE_WS_URL=

# 줄다리기 배틀(/tug-of-war-battle) 실시간 1:1 매칭 서버 주소. 미설정 시 개발 중에는
# ws://<현재 호스트>:8787/tug-of-war-battle 로 자동 접속을 시도한다.
# rps/liar/mafia/halligalli/yutnori/strategy-yutnori/mole-hunt/memory-sequence/updown-number/multiplication-sprint/odd-even-math/color-instruction/sum-ten-puzzle와 같은 서버 프로세스가 서비스하므로 WAS 배포 후에는 같은 도메인의 /tug-of-war-battle 경로를 지정한다.
# 자세한 내용은 ws-server/README.md 참고.
VITE_TUG_OF_WAR_BATTLE_WS_URL=

# 영역 쟁탈전(/territory-clash) 실시간 1:1 매칭 서버 주소. 미설정 시 개발 중에는
# ws://<현재 호스트>:8787/territory-clash 로 자동 접속을 시도한다.
# rps/liar/mafia/halligalli/yutnori/strategy-yutnori/mole-hunt/memory-sequence/updown-number/multiplication-sprint/odd-even-math/color-instruction/sum-ten-puzzle/tug-of-war-battle와 같은 서버 프로세스가 서비스하므로 WAS 배포 후에는 같은 도메인의 /territory-clash 경로를 지정한다.
# 자세한 내용은 ws-server/README.md 참고.
VITE_TERRITORY_CLASH_WS_URL=

# 라이트 게스(/light-guess) 실시간 매칭 서버 주소. 미설정 시 개발 중에는
# ws://<현재 호스트>:8787/light-guess 로 자동 접속을 시도한다.
# rps/liar/mafia/halligalli/yutnori/strategy-yutnori/mole-hunt/memory-sequence/updown-number/multiplication-sprint/odd-even-math/color-instruction/sum-ten-puzzle/tug-of-war-battle/territory-clash와 같은 서버 프로세스가 서비스하므로 WAS 배포 후에는 같은 도메인의 /light-guess 경로를 지정한다.
# 자세한 내용은 ws-server/README.md 참고.
VITE_LIGHT_GUESS_WS_URL=

# 리버시(/reversi) 실시간 1:1 매칭 서버 주소. 미설정 시 개발 중에는
# ws://<현재 호스트>:8787/reversi 로 자동 접속을 시도한다.
# rps/liar/mafia/halligalli/yutnori/strategy-yutnori/mole-hunt/memory-sequence/updown-number/multiplication-sprint/odd-even-math/color-instruction/sum-ten-puzzle/tug-of-war-battle/territory-clash/light-guess와 같은 서버 프로세스가 서비스하므로 WAS 배포 후에는 같은 도메인의 /reversi 경로를 지정한다.
# 자세한 내용은 ws-server/README.md 참고.
VITE_REVERSI_WS_URL=

# 오목(/gomoku) 실시간 1:1 매칭 서버 주소. 미설정 시 개발 중에는
# ws://<현재 호스트>:8787/gomoku 로 자동 접속을 시도한다.
# rps/liar/mafia/halligalli/yutnori/strategy-yutnori/mole-hunt/memory-sequence/updown-number/multiplication-sprint/odd-even-math/color-instruction/sum-ten-puzzle/tug-of-war-battle/territory-clash/light-guess/reversi와 같은 서버 프로세스가 서비스하므로 WAS 배포 후에는 같은 도메인의 /gomoku 경로를 지정한다.
# 자세한 내용은 ws-server/README.md 참고.
VITE_GOMOKU_WS_URL=
